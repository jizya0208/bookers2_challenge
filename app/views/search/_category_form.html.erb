  <%= form_with(url: category_search_path, method: :get, local: true) do |f| %>  <!-- methodをgetにすることでデータを渡しながらsearchビューへ遷移する。-->
    <%= f.text_field :category %>
    <%= f.submit '検索' %>
  <% end %>